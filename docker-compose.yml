services:
  hr-web:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: kuantum-hr-web
    ports:
      - "${HR_PORT:-5173}:5173"
    env_file:
      - .env.docker
    extra_hosts:
      - "host.docker.internal:host-gateway"
    restart: unless-stopped
